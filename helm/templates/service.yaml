apiVersion: v1
kind: Service
metadata:
  name: {{ include "catchx.fullname" . }}
  labels:
    {{- include "catchx.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "catchx.selectorLabels" . | nindent 4 }}
