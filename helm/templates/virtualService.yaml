apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ include "catchx.fullname" . }}
  labels:
    {{- include "catchx.labels" . | nindent 4 }}
spec:
  gateways:
    - {{ .Values.ezua.virtualService.istioGateway }}
  hosts:
    - {{ .Values.ezua.virtualService.endpoint | required "\nValid .Values.ezua.virtualService.endpoint is required !" }}
  http:
    - match:
        - uri:
            prefix: /
      rewrite:
        uri: /
      route:
        - destination:
            host: {{ include "catchx.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local
            port:
              number: 3000
